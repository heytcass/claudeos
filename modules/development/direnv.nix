{ ... }:

{
  # direnv is fully managed by home-manager in home/shell/cli-tools.nix
  # (programs.direnv with nix-direnv integration)
  #
  # This module intentionally left empty to avoid duplication.
  # System-level programs.direnv is not needed when home-manager handles it.
}
