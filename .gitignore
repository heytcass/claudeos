# Nix build results
result
result-*

# Editor/IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Secrets (unencrypted)
secrets/*.key
secrets/*.age
!secrets/.sops.yaml
!secrets/secrets.yaml

# Machine-local Claude Code settings (session-scratch allowlist; accretes
# one-off commands and machine paths — durable rules live in settings.json)
.claude/settings.local.json

# Temporary files
*.tmp
*.bak

# Git worktrees
.worktrees/
